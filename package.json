{
  "name": "minesweeper",
  "version": "0.1.0",
  "dependencies": {},
  "devDependencies": {
    "react-scripts": "1.0.17",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "styled-components": "^2.4.0"
  },
  "scripts": {
    "alias": "cd ./build && now alias && cd ../",
    "build": "react-scripts build",
    "deploy": "yarn build && now ./build && yarn alias",
    "start": "react-scripts start"
  }
}
